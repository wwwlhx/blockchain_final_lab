你现在不能再跟 AI 说“帮我变成全栈项目”了，因为从你这个项目现状看，它**已经有前端、后端、合约、CLI 和完整流程**，只是现在更像“能跑的原型系统”，还不够像“优秀的课程大作业 / 答辩项目”。目前明显短板包括：后端单文件 588 行、无数据库、元数据只存在本地文件、后端必须从 `contracts` 目录启动、无单元测试、无响应式布局、无认证/鉴权等。

你应该让 AI 做的不是“从零完成”，而是：

> **把现有原型升级成工程化、可答辩、有亮点的优秀全栈项目。**

你可以直接复制下面这段给 AI：

---

请你基于我当前的区块链数字资产确权系统项目，进行一次“优秀课程大作业级别”的重构和升级。

我现在的项目已经不是空项目，已经有：

1. Solidity 智能合约；
2. Hardhat 本地区块链；
3. Express + TypeScript 后端 API；
4. React + Vite + Tailwind 前端；
5. 文件上传、哈希计算、资产登记、校验、转移、撤销、历史追溯等功能。

但是现在的问题是：
项目虽然能跑，但还不够优秀、不够突出，更像一个原型系统。我希望你帮我把它升级成一个**工程结构清晰、功能完整、答辩亮点明显、前后端链路完整、视觉效果较好**的全栈项目。

请你不要推翻重写整个项目，而是在现有基础上做增强和重构。

## 一、请先分析当前项目的不足

请先从以下几个角度分析：

1. 工程结构是否合理；
2. 后端是否需要拆分 routes / controllers / services / database；
3. 当前没有数据库会带来什么问题；
4. 当前元数据只存在本地 JSON 文件有什么不足；
5. 当前后端依赖 Hardhat cwd 启动是否不够优雅；
6. 前端页面是否足够完整和有展示力；
7. 合约是否有测试、权限、事件追踪等不足；
8. 答辩时这个项目目前有哪些亮点，哪些地方还不够突出。

请先输出分析，不要直接写代码。

## 二、升级目标

请把这个项目升级成：

**区块链数字资产确权与可信流转平台**

要求它看起来像一个完整的全栈系统，而不是简单 demo。

最终项目需要体现以下亮点：

1. 区块链确权；
2. 三层哈希确权模型；
3. 文件完整性校验；
4. 链上资产生命周期追溯；
5. 链下数据库索引；
6. 元数据管理；
7. 前后端分离；
8. 钱包连接；
9. 可视化仪表盘；
10. 工程化后端结构；
11. 完整启动文档；
12. 可用于答辩演示。

## 三、请重点改进这些地方

### 1. 后端工程化重构

现在后端 `server.ts` 太长，所有 API 都在一个文件里。

请帮我拆分成：

```txt
backend/
├── src/
│   ├── app.ts
│   ├── server.ts
│   ├── routes/
│   │   ├── asset.routes.ts
│   │   ├── upload.routes.ts
│   │   ├── verify.routes.ts
│   │   ├── stats.routes.ts
│   │   └── chain.routes.ts
│   ├── controllers/
│   │   ├── asset.controller.ts
│   │   ├── upload.controller.ts
│   │   ├── verify.controller.ts
│   │   ├── stats.controller.ts
│   │   └── chain.controller.ts
│   ├── services/
│   │   ├── asset.service.ts
│   │   ├── blockchain.service.ts
│   │   ├── metadata.service.ts
│   │   ├── file.service.ts
│   │   └── stats.service.ts
│   ├── database/
│   │   ├── db.ts
│   │   ├── schema.sql
│   │   └── init.ts
│   ├── middleware/
│   │   ├── error.middleware.ts
│   │   └── upload.middleware.ts
│   └── utils/
│       ├── hash.ts
│       ├── response.ts
│       └── errors.ts
```

要求：

* 每个文件职责清晰；
* API 返回格式统一；
* 错误处理统一；
* 不要再把所有业务逻辑写在 `server.ts`；
* 保留原有功能；
* 给出每个新增文件的完整代码。

### 2. 增加 SQLite 数据库

当前项目没有数据库，资产列表完全依赖链上事件扫描，不够像完整全栈项目。

请帮我增加 SQLite 数据库，用于保存链下索引数据。

至少包括这些表：

```txt
users
assets
metadata
transactions
verification_records
system_logs
```

其中：

* 链上保存核心确权数据；
* 链下数据库保存展示用数据、文件信息、元数据详情、操作记录；
* 前端资产列表优先从数据库读取；
* 资产详情同时展示链上数据和链下数据；
* 历史记录同时结合链上事件和数据库 transactions 表。

请给我：

1. 数据库表结构；
2. 建表 SQL；
3. 初始化脚本；
4. 数据库操作封装；
5. 后端如何在登记、转让、撤销、验证时写入数据库。

### 3. 解耦 Hardhat 启动依赖

当前后端必须从 `contracts/` 目录启动，这不够优雅。

请帮我改成：

* 后端通过合约 ABI + 合约地址 + RPC URL 连接区块链；
* 不再依赖 `import { network } from "hardhat"`；
* 后端可以直接在 `backend/` 目录运行；
* 合约地址写入统一配置文件；
* 前端和后端都能读取部署后的合约地址。

请设计：

```txt
backend/src/config/blockchain.config.ts
frontend/src/config/contract.ts
contracts/deployments/localhost.json
```

并给出代码。

### 4. 增强前端展示效果

当前前端虽然有页面，但我希望更适合答辩展示。

请帮我优化前端 UI，让它更高级、更像完整系统。

重点升级：

1. Dashboard 增加数据统计卡片；
2. 增加链上 / 链下数据同步状态；
3. 增加三层确权模型可视化；
4. 增加资产生命周期时间线；
5. 增加文件哈希对比可视化；
6. 增加最近操作记录；
7. 增加系统架构说明页面；
8. 增加空状态、加载骨架屏、错误提示；
9. 增加响应式布局；
10. 增加演示模式 mock data，用于答辩展示。

前端风格要求：

* 科技感；
* 简洁；
* 暗色主题；
* 卡片式布局；
* 不要过度花哨；
* 适合老师现场查看；
* 每个页面都要有真实内容，不要空白页。

### 5. 增加钱包连接能力

现在后端默认使用 Hardhat 第一个账户，不够真实。

请帮我增加 MetaMask 钱包连接：

1. 前端连接 MetaMask；
2. 显示当前钱包地址；
3. 判断当前网络是否为本地 Hardhat；
4. 支持一键添加 / 切换本地网络；
5. 登记、转让、撤销操作尽量通过用户钱包签名；
6. 后端只负责文件、数据库、元数据和索引；
7. 链上写操作优先由前端通过 ethers.js 调用合约完成。

请说明哪些操作在前端完成，哪些操作在后端完成。

### 6. 增强智能合约测试

当前测试不够完整。

请帮我补全 Hardhat 单元测试，至少覆盖：

1. 正常登记资产；
2. 重复 fileHash 登记失败；
3. 查询资产；
4. owner 转让资产成功；
5. 非 owner 转让失败；
6. creator 撤销资产成功；
7. 非 creator / owner 撤销失败；
8. 已撤销资产不能转让；
9. 事件是否正确触发；
10. fileHash 反查 assetId。

请给出完整测试文件。

### 7. 增加答辩亮点文档

请帮我新增：

```txt
docs/
├── architecture.md
├── api.md
├── database.md
├── contract.md
├── demo-script.md
└── defense-highlights.md
```

其中 `defense-highlights.md` 要帮我总结这个项目的答辩亮点，包括：

1. 为什么要用区块链；
2. 三层确权模型的创新点；
3. 链上链下协同设计；
4. 文件哈希如何防篡改；
5. 为什么不能只用普通数据库；
6. 资产生命周期如何追溯；
7. 项目工程化体现在哪里；
8. 和普通 Web 系统相比有什么优势；
9. 项目的不足和未来优化方向。

## 四、不要做得太复杂

这个项目是课程期末大作业，不是商业系统。

请优先保证：

1. 能运行；
2. 能演示；
3. 能答辩；
4. 结构清晰；
5. 亮点突出。

不要引入太复杂的技术，比如：

* Docker 可以先不做；
* Kubernetes 不需要；
* 真正 IPFS 可以作为可选增强；
* 登录注册可以简单模拟；
* 权限系统不要过度复杂。

## 五、请按阶段输出

不要一次性输出所有代码。

请按下面顺序来：

第一阶段：分析当前项目不足，并给出升级路线图；
第二阶段：后端工程化重构；
第三阶段：SQLite 数据库接入；
第四阶段：区块链连接解耦；
第五阶段：前端 UI 和钱包连接升级；
第六阶段：合约测试补全；
第七阶段：README 和 docs 文档补全；
第八阶段：最终运行检查和答辩演示流程。

每个阶段都要告诉我：

1. 要新增哪些文件；
2. 要修改哪些文件；
3. 为什么这么改；
4. 给出完整代码；
5. 给出运行命令；
6. 给出测试方法。

请先执行第一阶段：
**分析当前项目不足，并给出升级路线图。不要先写代码。**

---

你还可以再补一句，让 AI 更懂你的目标：

> 我希望这个项目最后看起来不像“我临时拼出来的 demo”，而像一个结构清晰、有区块链特色、有工程化设计、有答辩亮点的完整全栈项目。

---

我建议你的改法优先级是这样：

```txt
第一优先级：后端拆分 + SQLite 数据库 + README
第二优先级：钱包连接 + 前端展示增强
第三优先级：合约测试 + docs 答辩文档
第四优先级：IPFS / 登录权限 / Docker
```

你现在的项目其实已经有基础亮点了，尤其是：

```txt
文件哈希
元数据哈希
链上确权记录
资产转移
资产撤销
历史追溯
前后端分离
```

真正欠缺的是“包装成一个优秀系统”的工程感。你让 AI 改的时候，关键词要从：

```txt
帮我完成全栈项目
```

改成：

```txt
请帮我把现有区块链确权原型升级为工程化、可答辩、有亮点的全栈系统。
```
